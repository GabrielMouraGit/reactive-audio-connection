version: "3.9"

services:
  app:
    build: .
    container_name: audio-chat-app
    restart: always
    environment:
      - NUXT_PUBLIC_SIGNALING_URL="https://audio.tytec.com.br/ws"
    ports:
      - "3000:3000"
      - "3005:3005"

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: always
    ports:
      - "8065:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app